# Context Kit — CHANGELOG

Append-only record of what's shipped.
Run `bash ~/agent-system/scripts/clawd-log.sh context-kit "shipped X"` to append.

---

## 2026-07-04

- 10:30 — v0.1 PUBLIC: pushed to github.com/JDDavenport/context-kit via API token workaround (gh CLI blocked in session); all 16 files live; M1 complete
- 09:00 — v0.1 initial build: 4 PCA templates (pca-wiki / pca-mental-models / pca-voice / pca-protocols) + domain-state template + CLAUDE.md.template + 5 skills (crm-everything / open-loops / watchers / morning-briefing / session-digest) + flagship essay (ESSAY.md) + one-command install.sh + README. Git commit f2c4022.
